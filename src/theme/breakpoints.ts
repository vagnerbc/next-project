import { createBreakpoints } from '@chakra-ui/theme-tools'

export const breakpoints = createBreakpoints({
  sm: '30em', // 480px in em
  md: '48em', // 768px in em
  lg: '62em', // 992px in em
  xl: '80em', // 1280px in em
  '2xl': '90em' // 1536px in em
})
